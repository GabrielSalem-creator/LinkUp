{
  "name": "linkup",
  "private": true,
  "scripts": {
    "build": "echo serving www",
    "vercel-build": "echo serving www",
    "export:web": "npm --prefix ./mobile run build && node scripts/sync-www.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}

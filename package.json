{
  "name": "linkup",
  "private": true,
  "scripts": {
    "build": "npm --prefix ./mobile run build",
    "vercel-build": "npm --prefix ./mobile run build"
  },
  "engines": {
    "node": ">=20"
  }
}

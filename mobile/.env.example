# Public (safe for Vercel + Expo client)
EXPO_PUBLIC_APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1
EXPO_PUBLIC_APPWRITE_PROJECT_ID=6a61202f000e451a56a8
EXPO_PUBLIC_APPWRITE_DATABASE_ID=linkup

# Force offline mock instead of Appwrite (optional)
# EXPO_PUBLIC_USE_MOCK=1

# Server-only — used by setup/seed scripts. NEVER put this in Vercel public env.
APPWRITE_API_KEY=
